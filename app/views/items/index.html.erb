<h1>Items#index</h1>


<!------------------------- LOGIN LOGOUT ---------------------------->

<ul>
  <% if user_signed_in? %>
    <li>
      <%= link_to 'Sign Out', destroy_user_session_path, method: :delete%>
    </li>
    <li>
      <%= current_user.email%>
    </li>
    <li>
      <%= current_user.address%>
    </li>
    <li>
      <%= link_to 'Edit Account', edit_user_registration_path %>
    </li>
  <% else %>
    <li>
      <%= link_to 'sign in', new_user_session_path %>
    </li>
    <li>
        <%= link_to 'sign UP', new_user_registration_path %>
    </li>
    <% end %>
</ul>

<p>

	<% @items.each do |item| %>
	<%= item.title %><br>
	<%= item.description %><br>
	<%= item.price %> €<br>
  <%= button_to 'Ajouter au panier', line_items_path(item_id: item), class: 'btn btn-primary' %>
</p>

<img src="<%= item.image_url %>"></img><br>

<a href="/show/:id">Voir l'article</a><% end %>

