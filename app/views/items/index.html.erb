<h1>Items#index</h1>

<!------------------------- LOGIN LOGOUT ---------------------------->
<ul>
  <% if user_signed_in? %>
    <li>
      <%= link_to 'Sign Out', destroy_user_session_path, method: :delete%>
    </li>
    <li>
      <%= current_user.email%>
    </li>
    <li>
      <%= current_user.address%>
    </li>
    <li>
      <%= link_to 'Edit Account', edit_user_registration_path %>
    </li>
  <% else %>
    <li>
      <%= link_to 'sign in', new_user_session_path %>
    </li>
    <li>
        <%= link_to 'sign UP', new_user_registration_path %>
    </li>
    <% end %>
</ul>

<!-------------------- AFFICHAGE DES ARTICLES --------------------->


<% @items.each do |item| %>

	<p>
		<%= item.title %><br>
		<%= item.description %><br>
		<%= item.price %> €<br>
	</p>

	<img src="<%= item.image_url %>"></img><br>
	<%= link_to "Afficher article", item_path(item.id) %>
        
<% end %>
 <!-------------------- AFFICHAGE DES ARTICLES --------------------->


